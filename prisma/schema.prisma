generator client {
  binaryTargets = ["native", "rhel-openssl-1.0.x"]
  provider      = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model visitors {
  ip_address String @id @db.VarChar(256)
  count      Int?
}
